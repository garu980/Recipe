import { navigation } from '../../presentation/navigation/localization/en';
import { recipeList } from '../../presentation/recipeList/localization/en';
import { createRecipe } from '../../presentation/createRecipe/localization/en';

const en = {
  ...navigation,
  ...recipeList,
  ...createRecipe,
};

export default en;
