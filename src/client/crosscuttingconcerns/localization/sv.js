import { navigation } from '../../presentation/navigation/localization/sv';
import { recipeList } from '../../presentation/recipeList/localization/sv';
import { createRecipe } from '../../presentation/createRecipe/localization/sv';
import { common } from '../../presentation/common/localization/sv';

const sv = {
  ...navigation,
  ...recipeList,
  ...createRecipe,
  ...common,
};

export default sv;
