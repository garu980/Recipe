import { navigation } from '../../presentation/navigation/localization/it';
import { recipeList } from '../../presentation/recipeList/localization/it';
import { createRecipe } from '../../presentation/createRecipe/localization/it';
import { common } from '../../presentation/common/localization/it';

const it = {
  ...navigation,
  ...recipeList,
  ...createRecipe,
  ...common,
};

export default it;
